#!/usr/bin/python3
def uniq_add(my_list=[]):
    """
    function that adds uniq num
    togeteher
    """
    new_list = []
    
    for num in my_list:
        if num not in new_list:
            add += num
            new_list.append(num)
        return add
